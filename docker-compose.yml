version: "3.2"

services:
    # web server
    php:
      build: ./build/php
      container_name: php-7.0-apache
      volumes:
        - ./src:/var/www/html
      networks:
        MyNetwork:
            ipv4_address: 192.168.43.10
      ports:
        - 80:80       
networks:
  MyNetwork:
    external: true
